# WARNING
# wal_compression = lz4 requires PostgreSQL
# to be compiled with --with-lz4
#
# io_method = io_uring requires PostgreSQL
# to be compiled with --with-liburing

# DB Version: 18
# OS Type: linux
# DB Type: web
# Total Memory (RAM): 3820 MB
# CPUs num: 2
# Data Storage: ssd

max_connections = 200
shared_buffers = 955MB
effective_cache_size = 2865MB
maintenance_work_mem = 244480kB
checkpoint_completion_target = 0.9
wal_buffers = 16MB
default_statistics_target = 100
random_page_cost = 1.1
effective_io_concurrency = 200
work_mem = 4701kB
huge_pages = off
jit = off
wal_compression = lz4
#io_method = io_uring
min_wal_size = 1GB
max_wal_size = 4GB

listen_addresses = '0.0.0.0'